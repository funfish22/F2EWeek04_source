import React from 'react';
import {storiesOf} from '@storybook/react';
import RadioLabel from '../RadioLabel';

storiesOf('organisms|RadioLabel', module).add('default', () => <RadioLabel>確認，我已暸解</RadioLabel>);
